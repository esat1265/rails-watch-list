<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <div class="justify-content-between collapse navbar-collapse" id="navbarSupportedContent">
        <%= link_to image_tag("logo", class: "custom-logo"), lists_path, class: "text-decoration-none" %>
        <div>

          <%= link_to "My List ", lists_path, class: "text-decoration-none" %>
          <%= link_to " New List", new_list_path,class: "text-decoration-none" %>

        </div>
      </div>
    </div>
  </nav>

  <div id="carouselExample" class="carousel slide">
    <div class="carousel-inner">
      <div class="carousel-item active">
        <%= image_tag("banner", class: "img-fluid")%>
      </div>
    </div>
  </div>
  <%= yield %>
  </body>
</html>
